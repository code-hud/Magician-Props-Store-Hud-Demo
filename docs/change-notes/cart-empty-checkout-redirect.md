# fix(cart): handle empty cart gracefully on checkout redirect

Redirect users back to the catalog with a friendly message when they hit checkout with an empty cart instead of rendering a blank order summary.
